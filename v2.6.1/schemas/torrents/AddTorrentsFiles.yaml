allOf:
  - type: object
    properties:
      torrents:
        type: array
        items:
          type: string
          format: binary
        description: Raw data of torrent file. `torrents` can be presented multiple times.
  - $ref: "./AddTorrentsCommon.yaml"
required:
  - torrents
